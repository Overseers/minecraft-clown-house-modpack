name: "Clown House Minecraft Modpack Pipeline"
on:
  - "push"
jobs:
  build-and-notify:
    runs-on: "ubuntu-latest"
    steps:
      - uses: "actions/checkout@v3"
      - run: "sudo apt install zip"
      - run: "rm README.md"
      - run: "ls"
      - run: "cd .."
      - run: "ls"
      # - run: "zip test.zip minecraft-clown-house-modpack/"
      - run: "echo TODO: Add zip step to create file"
      - run: "echo TODO: Add release file to GitHub"
      - run: "echo TODO: Add discord note"
      - run: "echo TODO: Update Curseforge"
      - uses: sebastianpopp/discord-action@releases/v1
        with:
          webhook: "${{secrets.DISCORD_WEBHOOK}}"
          message: "TEST"
